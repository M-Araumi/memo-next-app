export type Memo = {
    id: string;
    title: string;
    memoText: string;
};

export type ApiMemo = {
    id: string;
    title: string;
    memo_text: string;
    created_at: string;
    updated_at: string;
};